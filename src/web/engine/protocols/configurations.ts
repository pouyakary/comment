// ─── User Settings ─────────────────────────────────────────────────────── ✣ ─

export interface UserSettings {
  legacyMode:           boolean;
  ornament:             string;
  levelOneCommentWidth: number,
  levelTwoCommentWidth: number,
}